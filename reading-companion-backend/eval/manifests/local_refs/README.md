# Local Reference Manifests

Use this directory for tracked reference files that point to private/local corpora or local-only evaluation packages without checking those books or text-bearing packages into the repo.

These files should contain:
- stable local path references
- source ids
- optional fingerprints or file metadata
- notes about local-only availability
- optional local package refs for `state/eval_local_datasets/`
