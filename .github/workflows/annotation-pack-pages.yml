name: Contract Pages

on:
  pull_request:
    paths:
      - ".github/workflows/annotation-pack-pages.yml"
      - "contract/annotation-pack/**"
      - "contract/reading-product/**"
      - "scripts/build_annotation_pack_pages.py"
      - "scripts/build_contract_pages.py"
  push:
    branches:
      - main
      - codex/annotation-pack-v0
    paths:
      - ".github/workflows/annotation-pack-pages.yml"
      - "contract/annotation-pack/**"
      - "contract/reading-product/**"
      - "scripts/build_annotation_pack_pages.py"
      - "scripts/build_contract_pages.py"
  workflow_dispatch:

concurrency:
  group: annotation-pack-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Check Annotation Pack sub-projection
        run: python scripts/build_annotation_pack_pages.py --check
      - name: Check unified strict contract projection
        run: python scripts/build_contract_pages.py --check
      - name: Build Annotation Pack allowlist
        run: python scripts/build_annotation_pack_pages.py --output-dir _site
      - name: Add Reading Product allowlist
        run: python scripts/build_contract_pages.py --augment-output-dir _site
      - name: Upload Pages artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/configure-pages@v5
      - name: Deploy contract site
        id: deployment
        uses: actions/deploy-pages@v4
